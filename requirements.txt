# Основное — ставится через apt (рекомендуется):
#   sudo apt install python3-pyqt6 proxychains4
#
# Либо через pip (в venv):
PyQt6>=6.5
Pillow>=9  ; для перегенерации иконки (необязательно в рантайме)
